import "std/io";

fn main() {
    let x, y = 20, 10;


    if x > y {
        println("x is greater than y");
    }

    if x == 10 || y == 20 {
       println("Logical OR");
    }

    for i in 0..5 {
       println("This should print 5 times");
    }


    while x > 0 {
        println(`x: ${x}`);
        x = x - 1;
    }
} 