import "std/io";

struct User {
    id: i32,
    name: string,
    active: bool,
}

fn main() {
    let user = User {
        id: 1,
        name: "John",
        active: true,
    };

    println(user.name);
} 