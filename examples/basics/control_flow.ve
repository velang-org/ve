import "std/io";

fn main() {
    let x, y =
    10, 20;

    if x < y {
         print("x is less than y");
    }

    if x == 10 || y == 20 {
       print("Logical OR");
    }

    for i in 0..5 {
       print("This should print 5 times");
    }

    while x > 0 {
        print("x: " + x);
        x = x - 1;
    }
}
