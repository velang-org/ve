import "std/io";

fn main() {
    let num, boolean, text, pi =
    10, true, "Types in Verve", 3.1415;

    println(`Number: ${num}`);
    println(`Boolean: ${boolean}`);
    println(`Text: ${text}`);
    println(`Float: ${pi}`);
}

