import "std/io";

enum Color {
    Red,
    Green,
    Blue
}


fn main() {
    let test = Color.Red;

    let value = match test {
        Color.Red => "czerwony",
        Color.Green => "zielony",
        Color.Blue => "niebieski",
    };

    println(value);
}v