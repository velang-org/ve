import "std/io";

enum Color {
    Red,
    Green,
    Blue
}


fn main() {
    let colour = Color.Red;

    let value = match colour {
        Color.Red => "red",
        Color.Green => "green",
        Color.Blue => "blue",
    };

    println(value);
}