import "std/io";

enum Color {
    Red,
    Green,
    Blue
}


fn main() {
    let test = Color.Red;

    let value = match test {
        Color.Red => "red",
        Color.Green => "green",
        Color.Blue => "blue",
    };

    println(value);
}