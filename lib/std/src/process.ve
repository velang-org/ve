

struct Command {
    program: string,
    args: string[],
}

impl Command {
    fn new(program: string) -> Command {
        Command {
            program: program,
            args: [],
        }
    }
}